rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // All documents are protected and handled securely
    match /documents/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
